* Command to run code for use-case 1:
	mvn compile exec:java -Dexec.mainClass="com.iiht.training.reverse.ReverseVowels"

* Command to run code for use-case 2:
	mvn compile exec:java -Dexec.mainClass="com.iiht.training.trimwhitespaces.TrimWhiteSpaces"

* Mandatory: Run the following command before the final submission
	mvn test
